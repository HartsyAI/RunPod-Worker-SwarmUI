# SwarmUI RunPod Serverless - Minimal Dependencies

# RunPod SDK (required for serverless handler)
runpod>=1.6.2

# Vast.ai's own SDK (vast_worker.py's dependency) is intentionally NOT here - it pins
# cryptography==49.0.0 exactly, which fights the Debian-managed cryptography package this
# image already has and breaks this install. It gets its own isolated venv instead; see the
# Dockerfile's "Install Vast.ai Serverless Dependencies" step.

# HTTP requests with retry capabilities
requests>=2.31.0
urllib3>=2.0.0

# Environment variable management (for local testing)
python-dotenv>=0.21.0