# SwarmUI RunPod Serverless - Environment Variables

# ============================================================================
# Handler Configuration (Container Runtime)
# ============================================================================
SWARMUI_HOST=0.0.0.0
SWARMUI_PORT=7801
SWARMUI_API_URL=http://127.0.0.1:7801

# Network Volume Configuration
VOLUME_PATH=/runpod-volume

# Startup Configuration
STARTUP_TIMEOUT=1800        # Seconds to wait for SwarmUI to boot (30 min)

# ============================================================================
# Testing Configuration (Local Development)
# ============================================================================
# RunPod Endpoint (for running tests)
RUNPOD_ENDPOINT_ID=your-endpoint-id
RUNPOD_API_TOKEN=your-runpod-api-token

# ============================================================================
# Optional Configuration
# ============================================================================
# Hugging Face Token (if downloading gated models via external tools)
HUGGINGFACE_API_TOKEN=your-huggingface-token
