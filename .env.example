# SwarmUI RunPod Serverless Environment Variables

# SwarmUI Server Configuration
SWARMUI_HOST=0.0.0.0
SWARMUI_PORT=7801

# Network Volume Configuration
VOLUME_PATH=/runpod-volume

# Handler Configuration
SWARMUI_API_URL=http://127.0.0.1:7801
STARTUP_TIMEOUT=1800        # Seconds to wait for SwarmUI to boot
GENERATION_TIMEOUT=600      # Seconds to wait for a generation request

# RunPod Serverless Credentials
RUNPOD_ENDPOINT_ID=your-endpoint-id
RUNPOD_API_TOKEN=your-runpod-api-token

# RunPod S3 / Network Volume Access
RUNPOD_ENDPOINT_URL=https://s3api-REGION.runpod.io/
RUNPOD_ACCESS_KEY=your-runpod-access-key
RUNPOD_SECRET_ACCESS_KEY=your-runpod-secret-key
RUNPOD_TRAINING_STORAGE_REGION=REGION
RUNPOD_TRAINING_STORAGE_VOLUME_ID=your-volume-id
# Optional prefix inside the bucket for storing models (leave blank for root)
RUNPOD_S3_PREFIX=

# Hugging Face Token (required for gated models)
HUGGINGFACE_API_TOKEN=your-huggingface-token

# Optional: Auto-download models on first start
AUTO_DOWNLOAD_MODELS=false

# Optional: Default model to use if not specified
# Use SwarmUI model identifier (folder/subfolder/filename without extension)
DEFAULT_MODEL=OfficialStableDiffusion/sd_xl_base_1.0