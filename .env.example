# SwarmUI RunPod Serverless sample environment file

# Set the default SwarmUI host/port (override defaults defined in Dockerfile)
SWARMUI_HOST=0.0.0.0
SWARMUI_PORT=7801

# Path where the RunPod network volume is mounted inside the container
VOLUME_PATH=/runpod-volume

# Increase if your initial install takes longer than 10 minutes
STARTUP_TIMEOUT=900

# Optional: auto-download default models in start.sh (true/false)
AUTO_DOWNLOAD_MODELS=false

# Optional: custom arguments for ComfyUI installer or runtime
# COMFY_ARGS=--highvram
